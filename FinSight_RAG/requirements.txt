# ── UI layer ─────────────────────────────────────────────────────────────────
streamlit>=1.34
pandas>=2.1
plotly>=5.20
pydantic>=2.6

# ── Financial data ────────────────────────────────────────────────────────────
yfinance>=0.2.37

# ── RAG pipeline ─────────────────────────────────────────────────────────────
feedparser>=6.0.11          # RSS feed parsing (Bloomberg, Reuters, CNBC, etc.)
requests>=2.31.0            # HTTP requests with connection pooling
scikit-learn>=1.3.0         # TF-IDF vectorisation + cosine similarity retrieval
numpy>=1.24.0               # Array maths for TF-IDF scoring

# ── Agent orchestration ───────────────────────────────────────────────────────
langgraph>=0.2.0            # LangGraph StateGraph for the 3-agent workflow
langchain>=0.3.0            # LangChain core (agent building blocks)
langchain-core>=0.3.0       # Required by langgraph

# ── LLM backend (optional — only needed for GPT-4o-mini mode) ─────────────────
langchain-openai>=0.2.0     # OpenAI structured-output client
openai>=1.30.0              # OpenAI Python SDK

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0        # .env file loading
